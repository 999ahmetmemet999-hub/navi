{
  "_comment": "Reference truck custom model for GraphHopper. Useful if you self-host a GraphHopper server instead of ORS, or as the build-time profile for a future offline (Valhalla/GraphHopper) engine. These filters exclude roads whose OSM max_* tags are below the truck's dimensions, and roads where heavy goods vehicles are banned (hgv=no). Tune the numbers to your fleet.",
  "priority": [
    { "if": "hgv == NO", "multiply_by": "0" },
    { "if": "max_height < 4.0", "multiply_by": "0" },
    { "if": "max_width < 2.55", "multiply_by": "0" },
    { "if": "max_length < 16.5", "multiply_by": "0" },
    { "if": "max_weight < 40", "multiply_by": "0" },
    { "if": "road_class == TRACK", "multiply_by": "0.1" }
  ],
  "speed": [
    { "if": "true", "limit_to": "85" },
    { "if": "road_class == RESIDENTIAL", "limit_to": "30" }
  ]
}
